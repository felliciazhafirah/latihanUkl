# Latihan UKL

Proyek ini digunakan untuk latihan ujian kompetensi keahlian (UKL) menggunakan **Node.js**, **Express**, dan **Prisma ORM**.

## 📂 Struktur Folder
- `kehadiran.controller.js` → Controller untuk fitur kehadiran  
- `user.controller.js` → Controller untuk data user  
- `*.route.js` → File routing Express  
- `schema.prisma` → Konfigurasi Prisma untuk database  
